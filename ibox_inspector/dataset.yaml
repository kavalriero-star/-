# Ultralytics YOLO 학습용 데이터셋 정의 (단일 클래스: product)
#
# 라벨링 후 아래 경로에 이미지/라벨을 배치하세요:
#   data/dataset/images/  (학습 이미지 .jpg/.png)
#   data/dataset/labels/  (YOLO 형식 .txt: "0 cx cy w h", 0~1 정규화)
#
# 데이터가 적으면 train/val 을 같은 폴더로 두어도 됩니다(과적합 주의).

path: data/dataset
train: images
val: images

names:
  0: product
