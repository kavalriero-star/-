# DESIGN.md — 엘레멕 생산관리 프로그램 디자인 기준

이 문서는 화면을 만들 때마다 매번 취향을 다시 묻지 않기 위한 **고정 기준**입니다.
Claude를 포함해 이 저장소에서 UI를 작업하는 모든 사람이 시작 전에 이 파일을 읽습니다.

- 여기 적힌 값은 **결정된 것**입니다. 바꾸려면 이 파일을 고치고 이유를 커밋 메시지에 남깁니다.
- "확정 필요" 절의 항목은 아직 정해지지 않았습니다. 그 부분만 물어보면 됩니다.

---

## 1. 이 제품의 성격

**내부 업무용 생산관리 시스템.** 공장에서 일하는 사람이 하루 종일 보는 화면입니다.

| 항목 | 내용 |
| --- | --- |
| 화면 종류 | 대시보드, 데이터 표, 입력 폼, 상태 모니터링 |
| 사용자 | 현장 작업자, 생산 관리자, 사무 인력 |
| 사용 환경 | 밝은 공장 조명, 사양이 낮을 수 있는 PC, 장시간 연속 사용 |
| 언어 | 한국어 |
| 판단 기준 | **예쁨보다 정확함.** 숫자를 잘못 읽으면 안 되는 화면입니다 |

이 성격에서 따라 나오는 원칙 세 가지:

1. **정보 밀도를 낮추지 않는다.** 여백을 벌려 화면당 정보를 줄이면 사용자는 스크롤과 페이지 이동을 더 하게 됩니다. 마케팅 페이지의 넉넉한 여백 감각을 그대로 가져오지 않습니다.
2. **상태는 색 하나로만 표시하지 않는다.** 색약 사용자와 밝은 조명에서 색만으로는 구분되지 않습니다. 항상 색 + 글자(또는 아이콘 모양)를 함께 씁니다.
3. **화려한 움직임을 넣지 않는다.** 장시간 보는 화면에서 애니메이션은 피로와 오작동의 원인입니다.

---

## 2. 설치된 스킬을 이 프로젝트에서 어떻게 쓰는가

다섯 개 스킬이 `.claude/skills/`에 설치되어 있지만, **이 프로젝트에 다 해당되지는 않습니다.**

| 스킬 | 이 프로젝트에서 | 이유 |
| --- | --- | --- |
| `web-design-guidelines` | **주력.** UI 코드를 쓰면 항상 통과시킴 | 접근성·폼·표·성능 규칙. 업무용 화면에 그대로 맞음 |
| `agent-browser` | **주력.** 만든 화면을 열어 보고 고침 | 눈으로 확인하지 않은 화면은 완성이 아님 |
| `frontend-design` | **부분 적용** | 아래 참고 |
| `design-taste-frontend` | **거의 해당 없음** | 스킬 문서가 스스로 적용 범위를 "랜딩 페이지·포트폴리오·리디자인, 대시보드와 데이터 표는 제외"로 못박고 있음 |
| `image-to-code` | **제한적** | 회사 소개 페이지나 로그인 화면처럼 시각적 인상이 중요한 화면에만 |

### `frontend-design`을 부분 적용하는 이유

Anthropic 공식 스킬이고 "AI 티 나는 기본값"을 피하는 데 유용하지만, 방향이 둘로 나뉩니다.

**이 프로젝트에 쓸 것**
- 자기 비평: 만들면서 스스로 점검하고, 장식 하나를 덜어내기
- 품질 하한선: 모바일 대응, 키보드 포커스 보이기, `prefers-reduced-motion` 존중
- AI 기본값 회피: 크림색 배경 + 고대비 세리프 + 테라코타, 검정 배경 + 형광 강조색 같은 조합을 피하기
- 카피 작성: 문구가 템플릿처럼 읽히지 않게

**이 프로젝트에 쓰지 않을 것**
- "기억에 남을 시그니처 요소 하나", "정당화 가능한 미적 모험 하나"
- 히어로 섹션을 주장(thesis)으로 삼는 구성
- 연출된 페이지 로드 시퀀스, 스크롤 트리거 애니메이션

작업 지시가 생산관리 화면이면, 이 스킬의 "모험" 부분보다 **1장의 세 가지 원칙**(정보 밀도 유지 · 색만으로 상태 표시 금지 · 화려한 움직임 금지)이 우선합니다. 충돌하면 1장이 이깁니다.

> `frontend-design`·`design-taste-frontend`·`image-to-code` 세 개는 나중에 대외용 페이지(회사 소개, 제품 소개)를 만들 때 본래 용도로 쓰면 됩니다. 생산관리 화면에서는 위에 적힌 범위로만 씁니다.

**작업 순서**

1. `DESIGN.md`(이 파일)를 읽고 토큰을 확인한다
2. 화면을 만든다
3. `agent-browser`로 실제로 띄워서 본다
4. `web-design-guidelines`로 코드를 점검한다
5. 3~4에서 나온 문제를 고치고 다시 확인한다

---

## 3. 색

모든 값은 WCAG 대비를 실제로 계산해 검증했습니다. 괄호 안은 흰 배경(`#FFFFFF`) 기준 대비입니다.

### 바탕과 글자

```css
:root {
  --bg:            #FFFFFF;  /* 기본 바탕 */
  --bg-sunken:     #FAF9F7;  /* 표 머리글, 패널 바탕 */
  --text:          #1C1A17;  /* 본문   (17.36:1) */
  --text-muted:    #57534E;  /* 보조   ( 7.63:1) */

  --border:        #E3E0DB;  /* 표 구분선 등 장식용 */
  --border-strong: #8C857C;  /* 입력칸·컨트롤 테두리 (3.65:1) */
}
```

`--border`(장식)와 `--border-strong`(컨트롤)을 **구분해서** 쓰세요.
입력칸·체크박스·셀렉트 테두리에 연한 `--border`를 쓰면 접근성 기준(비텍스트 3:1)에 미달합니다.

### 액션

```css
:root {
  --accent:      #1F5F8B;  /* 기본 버튼, 링크, 포커스 링 (6.84:1) */
  --accent-text: #FFFFFF;  /* --accent 위 글자        (6.84:1) */
}
```

### 상태 — 생산관리의 핵심

```css
:root {
  --st-ok:      #16794C;  /* 정상 · 완료 · 합격 (5.42:1) */
  --st-warn:    #A15C07;  /* 주의 · 지연       (5.19:1) */
  --st-error:   #B42318;  /* 이상 · 불량 · 정지 (6.57:1) */
  --st-idle:    #57534E;  /* 대기 · 미가동      (7.63:1) */
}
```

**상태 표시 규칙 (반드시 지킬 것)**

```html
<!-- 이렇게 (색 + 글자) -->
<span class="badge badge--error">불량</span>

<!-- 이렇게 하지 말 것 (색만) -->
<span class="dot" style="background: var(--st-error)"></span>
```

빨간 점 하나로 "불량"을 표시하면 색약 사용자는 읽지 못합니다. 점을 쓰더라도 옆에 반드시 글자를 붙입니다.

### 다크 모드

**지금은 만들지 않습니다.** 공장 조명 환경에서 밝은 화면이 기본입니다.
나중에 필요해지면 `<html>`에 `color-scheme: dark`를 주고 위 토큰만 다시 정의합니다(구조는 그대로).

---

## 4. 글자

```css
:root {
  --font-sans: system-ui, -apple-system, "Apple SD Gothic Neo",
               "Malgun Gothic", "맑은 고딕", sans-serif;
  --font-mono: ui-monospace, "SFMono-Regular", Consolas, monospace;
}
```

시스템 글꼴을 씁니다. 웹폰트를 받아오지 않으므로 저사양 PC와 사내망에서도 즉시 뜹니다.

| 용도 | 크기 / 굵기 | 비고 |
| --- | --- | --- |
| 화면 제목 | 20px / 600 | |
| 구역 제목 | 16px / 600 | |
| 본문·표 내용 | 14px / 400 | 업무 화면 기준 |
| 표 머리글 | 13px / 600 | |
| 보조 설명 | 13px / 400 | `--text-muted` |

**14px 미만은 쓰지 않습니다.** 현장에서 읽히지 않습니다.

### 숫자 — 이 프로젝트에서 특히 중요

```css
.num, td.num, th.num {
  font-variant-numeric: tabular-nums;
  text-align: right;
}
```

수량·불량률·시간이 세로로 늘어서는 화면입니다. `tabular-nums`가 없으면 자릿수가 어긋나 보여 오독이 생깁니다. **숫자 열에는 예외 없이 적용합니다.**

날짜·숫자 형식은 직접 만들지 말고 `Intl.DateTimeFormat` / `Intl.NumberFormat`을 씁니다.

### 문장 부호

- 줄임표는 `…` (마침표 세 개 `...` 아님)
- 로딩 문구는 `…`로 끝냄: `저장 중…`, `불러오는 중…`
- 단위는 붙여쓰기 방지: `120&nbsp;개`, `98.2&nbsp;%`

---

## 5. 간격과 배치

4px 배수만 씁니다.

```css
:root {
  --sp-1: 4px;   --sp-2: 8px;   --sp-3: 12px;
  --sp-4: 16px;  --sp-6: 24px;  --sp-8: 32px;
}
```

- 모서리 둥글기: `4px` (버튼·입력칸), `6px` (패널). 그 이상은 쓰지 않습니다.
- 그림자: 떠 있는 것(드롭다운, 모달)에만. 카드에는 테두리를 쓰고 그림자는 쓰지 않습니다.
- 배치는 flex/grid로 합니다. JS로 위치를 계산하지 않습니다.

---

## 6. 컴포넌트 규칙

### 표 — 이 제품의 중심

- 머리글은 `<th>`, 고정할 때 `position: sticky; top: 0`
- 숫자 열은 오른쪽 정렬 + `tabular-nums`
- **50행이 넘으면 가상 스크롤**을 씁니다. 통째로 `.map()` 하지 않습니다
- 빈 결과에 빈 표를 그리지 말고 "조건에 맞는 항목이 없습니다"를 보여줍니다
- 긴 텍스트 셀은 `truncate` + 툴팁, flex 자식에는 `min-w-0`

### 폼

- 모든 입력칸에 `<label>`. `htmlFor`로 연결해 라벨을 눌러도 포커스가 가게 합니다
- `type`과 `inputmode`를 맞게 씁니다 (수량은 `type="number"`, 전화는 `tel`)
- **붙여넣기를 막지 않습니다.** 현장에서 코드·품번을 복사해 넣습니다
- 품번·사번·코드 입력칸은 `spellCheck={false}`
- 제출 버튼은 요청이 시작되기 전까지 활성 상태로 둡니다
- 오류는 해당 칸 바로 옆에 표시하고, 제출 시 첫 오류로 포커스를 옮깁니다
- 저장하지 않은 변경이 있으면 이동 전에 경고합니다

### 버튼

- 동작은 `<button>`, 이동은 `<a>`. `<div onClick>` 금지
- 아이콘만 있는 버튼에는 `aria-label` 필수
- 라벨은 구체적으로: `저장`이 아니라 `작업지시 저장`
- `hover` 상태를 반드시 넣습니다

### 포커스

```css
:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
}
```

`outline: none`을 대체 표시 없이 쓰지 않습니다. 고정 헤더가 포커스된 요소를 가리지 않게 합니다.

### 되돌릴 수 없는 동작

작업지시 삭제, 실적 마감, 재고 조정처럼 되돌리기 어려운 동작은 **확인 창이나 취소 시간**을 반드시 둡니다. 즉시 실행하지 않습니다.

### 움직임

- `prefers-reduced-motion`을 존중합니다
- `transform`과 `opacity`만 애니메이션합니다. `transition: all` 금지
- 길이는 150ms 이내. 상태 전환에만 씁니다

---

## 7. 화면을 넘기기 전 점검

```
[ ] agent-browser로 실제로 띄워서 봤다
[ ] 숫자 열에 tabular-nums가 들어갔다
[ ] 상태를 색만으로 표시한 곳이 없다
[ ] 키보드 Tab만으로 모든 기능을 쓸 수 있다
[ ] 포커스 링이 보인다
[ ] 표가 비었을 때 화면이 깨지지 않는다
[ ] 아주 긴 품명을 넣어도 레이아웃이 무너지지 않는다
[ ] web-design-guidelines 점검을 통과했다
```

---

## 8. 쓰지 않는 것

AI가 만든 티가 나는 기본값들입니다. 이 프로젝트에서는 쓰지 않습니다.

- 보라색 그라데이션, 어두운 배경 위 가운데 정렬 히어로
- 유리질(glassmorphism) 효과
- 똑같이 생긴 카드 3개 나열
- 끝없이 반복되는 미세 애니메이션
- 이모지를 상태 표시로 사용 (🟢🔴 → `정상` `이상`)
- 과한 둥근 모서리(12px 이상), 큰 그림자
- 의미 없는 작은 배지·태그·라벨 남발

---

## 9. 확정 필요 — 정해지면 이 문서를 갱신할 것

아래는 아직 정해지지 않았습니다. 임의로 넣지 말고 담당자에게 확인하세요.

| 항목 | 필요한 결정 |
| --- | --- |
| 회사 브랜드 색 | 엘레멕 공식 색이 있으면 `--accent`를 교체 (교체 시 대비 4.5:1 재검증) |
| 로고 | 파일과 최소 여백 규정 |
| 지원 브라우저 | 현장 PC의 브라우저·버전 범위 |
| 화면 해상도 | 현장 모니터 최소 해상도 (레이아웃 최소 폭 결정에 필요) |
| 기술 스택 | 프레임워크가 정해지면 위 CSS 토큰을 해당 방식으로 옮김 |

---

## 참고

접근성·폼·성능 세부 규칙의 원문은 `web-design-guidelines` 스킬이 실행 시점에 받아옵니다.
이 문서는 그 규칙 중 **이 프로젝트에 특히 중요한 것**을 추린 뒤, 제품 성격에 맞는 결정을 더한 것입니다.
